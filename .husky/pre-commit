#!/usr/bin/env sh

# Run lint-staged first to ensure files are properly formatted
npx lint-staged

# Then check for CHANGELOG.md updates in the final staged files
if ! git diff --cached --name-only | grep -q "CHANGELOG\.md"; then
  echo "Error: CHANGELOG.md must be updated with your changes"
  exit 1
fi
